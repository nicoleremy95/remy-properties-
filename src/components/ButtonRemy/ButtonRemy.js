import React from 'react';
import { makeStyles } from '@material-ui/core/styles';
import Button from '@material-ui/core/Button'; 

export default function ButtonRemy() {
    return (
        <div>
            <Button variant="contained" color="primary">
        Contact Us
      </Button>
        </div>
    )
}
